{
  "name": "@akcp/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:mock": "tsx src/mock-server.ts",
    "build": "echo 'no build needed'",
    "lint": "echo 'no lint needed'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests'",
    "clean": "node -e \"fs.rmSync('dist', {recursive:true,force:true}); fs.rmSync('*.tsbuildinfo', {force:true})\""
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "@types/express": "^5.0.0",
    "tsx": "^4.19.0"
  }
}
