# @akcp/mcp-automation-server

Stateful, Human-in-the-Loop (HITL) MCP server for browser automation (Playwright).
Requires explicit opt-in and approval tokens to execute side-effects. Defaults to sandbox mode.
